namespace KOP.Common.Enums
{
    public enum StatusCodes
    {
        OK = 200,
        BadRequest = 400,
        EntityNotFound = 404,
        InternalServerError = 500,
        IncorrectPassword = 401,
        UserIsSuspended = 403,
    }
}